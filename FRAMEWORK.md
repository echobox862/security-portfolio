# 两条判断框架

> 这个仓库里的每一篇笔记，都是这两条框架的一个实例。
>
> 写笔记的顺序是：先判断"它归到哪条框架"，再写现象和步骤。这样留下来的是一套能迁移到新目标的判断，而不是一堆需要背下来的 payload。

---

## 框架一：getshell 三元组

**能写进去 + 能访问到 + 能被解析。**

三个条件同时成立，利用才成立；缺任意一条，链条就断在那里。判断一条"写入类"漏洞能不能打到命令执行，就是把这三条逐一过一遍。

| 路径 | 能写进去 | 能访问到 | 能被解析 |
|---|---|---|---|
| **文件上传** | 上传接口接受了内容不被清洗的文件 | 落盘路径可从 Web 访问（目录在站点根下、或用路径穿越把文件放到站点根） | 后缀落在服务端脚本解析范围内（`.php`/`.jsp`/`.phtml`…），且目标目录允许执行 |
| **WAR 部署**（[01](writeups/01-tomcat-weak-password-getshell.md)） | Manager 的 deploy 功能本身就是为写文件设计的 | 包名成为上下文路径，包内文件名成为访问名，路径可推导 | 包内放 `.jsp`，由 Tomcat 自身解析 |
| **`into outfile` 写 webshell**（[02](writeups/02-sqli-labs-less1-10.md) Less 7） | 数据库账号有 `FILE` 权限，且 `secure_file_priv` 允许写目标目录 | 写入路径必须落在 Web 根目录内（本实验靠 `..\` 从数据目录跳出去） | 写入的内容是服务端脚本，且该目录允许执行 |

### 为什么它是"三个条件"而不是"一个漏洞"

因为这三条**分属三个不同的组件**：写入能力来自业务功能，访问能力来自 Web 服务器与目录配置，解析能力来自服务端语言与解析器配置。任何一条都由不同的人、在不同时间配置——所以它们**经常不同时成立**，也经常在加固时只补上其中一条。

这带来两个直接结论：

- **测试时**：把它当检查表。写不进去就找别的写入通道；写进去了访问不到就找路径泄露或穿越；能访问但被当静态文件下载，就去试换后缀、加解析配置。
- **防御时**：打断任意一条即可，而且**打断"能被解析"最省事**——把上传目录、数据目录设成禁止脚本执行，写入类漏洞的收益就归零了。这比逐个去过滤上传内容更可靠。

### 一个容易记错的点

「能写进去」不等于「上传成功」。文件上传返回 200 只说明第 1 条满足；**真正要验证的是第 3 条**——访问一次，看返回的是文件内容还是被执行的输出。

---

## 框架二：绕过的统一原理

**过滤发生在字符串层，解析发生在语义层；两者不一致的地方，就是缝隙。**

各种听起来不同的绕过手法，本质上都是同一件事：**过滤器检查的字符串形态，和程序最终解析出的语义，不是同一个东西**。只要这两者之间存在可构造的差异，过滤就能被绕过。

| 形态 | 过滤器看到的是 | 程序最终执行/落点 | 本仓库实例 |
|---|---|---|---|
| **关键词双写** | 输入的中间夹了一层"多余"字符，看起来不含敏感词 | 过滤只做一次替换、且替换为空，反而把多余层消掉、还原出关键词 | [04](writeups/04-xss-labs-1-7.md) Less 3 / 5（`alert` 类关键词） |
| **变量拼接 / 参数交叉引用** | 每个参数单独看都是无害碎片 | 程序把这些碎片拼回完整表达式后再解析，危险词完整出现 | [03](writeups/03-haiyang-cms-param-injection-rce.md)（参数互相引用拼回执行语句） |
| **路径穿越 `..\`** | 路径字符串以"允许目录"开头，前缀检查通过 | 文件系统把 `..` 消解掉，实际落点在允许目录之外 | [02](writeups/02-sqli-labs-less1-10.md) Less 7 |
| **`%00` 截断** | 字符串长度被截断，被检查的后半段不存在 | 底层按 C 字符串规则在空字节处终止，语义与字符串层不一致 | 历史手法；现代语言与框架多已修复 |
| **编码 / 大小写 / 空白变体** | 命中不了关键词的正则 | 解析阶段等价于原关键词 | 与双写同源，见 [04](writeups/04-xss-labs-1-7.md) Less 6 |

### 这条原理的用处：从"试 payload"改成"先猜实现"

它把绕过从"碰运气"变成"先逆推再构造"：

1. 先想清楚**这个位置的过滤是字符串操作还是语义操作**——是 `replace` 一次？是黑名单正则？是前缀判断？
2. 再想清楚**程序下一层会怎么解析这串字符**——它会不会做二次替换、会不会消解路径、会不会按字节长度判断？
3. 两者的差异点就是可构造的位置。构造完还要**验证解析结果是否正确**，而不是只看"有没有弹窗/有没有报错"。

[04](writeups/04-xss-labs-1-7.md) Less 6 是最清楚的一个例子：过滤**按顺序依次删除**多个关键词，所以构造时必须考虑删除的先后关系——**顺序错了就拼不出目标**。这说明绕过要精确，不能靠穷举。

### 它同时解释了"为什么黑名单不行"

黑名单只能列举已知危险项，而"能达到同一效果的手段"枚举不完（[04](writeups/04-xss-labs-1-7.md) Less 4 用一次函数替换就证明了这点）。而任何**只做字符串层过滤**的方案，都会在这条原理下被反复绕过。

所以修复的重心不是"把过滤写得更全"，而是**让字符串层与语义层合一**：

| 漏洞类型 | 消解差异的做法 |
|---|---|
| SQL 注入 | 参数化查询 / 预编译——让数据永远不进"字符串拼接"这一步 |
| XSS | 按输出上下文编码——让数据在语义上永远是数据，不是标签或脚本 |
| 路径穿越 | 路径规范化（消解 `..`）后再做前缀判断，而不是先判断再交给文件系统 |
| 命令执行 | 不经 shell 执行、参数数组传递、白名单枚举 |

---

## 两条框架怎么配合用

它们管的不是同一件事：

- **框架一**回答"**这条链能不能打通**"——是能力边界问题，检查表式判断，结论是"通/不通"。
- **框架二**回答"**为什么过滤器没拦住**"——是实现缺陷问题，逆推式判断，结论是"缝隙在哪里、怎么补"。

一篇笔记的「原理」一节把这两条走完，「修复」一节就自然有了层次：**先用框架二定位补齐方式（让过滤与解析一致），再用框架一决定打断哪一环（改配置最彻底）。**

---

## 覆盖情况（本仓库）

| 框架 | 路径 / 形态 | 覆盖 |
|---|---|---|
| getshell 三元组 | WAR 部署 | [01](writeups/01-tomcat-weak-password-getshell.md) |
| getshell 三元组 | `into outfile` 写 webshell | [02](writeups/02-sqli-labs-less1-10.md) Less 7 |
| getshell 三元组 | 文件上传 | 暂无单独成篇的笔记 |
| 绕过原理 | 关键词双写 | [04](writeups/04-xss-labs-1-7.md) Less 3 / 5 / 6 |
| 绕过原理 | 变量拼接与参数交叉引用 | [03](writeups/03-haiyang-cms-param-injection-rce.md) |
| 绕过原理 | 路径穿越 | [02](writeups/02-sqli-labs-less1-10.md) Less 7 |
| 绕过原理 | 编码与截断类 | 部分覆盖（[02](writeups/02-sqli-labs-less1-10.md) 的 URL 编码、[04](writeups/04-xss-labs-1-7.md) 的关键词变形） |

补充说明：本仓库的实例来自 2025 年 6–7 月的授权靶场练习，因此覆盖范围受当时的实验内容限制；文件上传、编码截断这两块目前只有框架描述，没有对应笔记。
